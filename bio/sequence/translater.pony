use "../alphabet"

// primitive Translater
//   fun apply(rna: RNASequence box): ProteinSequence ref =>
//     let res = Array[AminoAcid](rna.seq.size())
//     for letter in rna.seq.values() do
//       res.push(match letter
//         // TODO: Use the standard codon table
//       end)
//     end
//     ProteinSequence(res)

